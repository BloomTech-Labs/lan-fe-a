import styled from 'styled-components';
import { Layout } from 'antd';

// Styles for the header
export const DashboardHeaderStyles = styled(Layout.Header)`
  width: 100%;
  border-bottom: 1px solid rgba(0, 0, 0, 0.08);
`;
